# 改写示例

## 删除铺垫

改写前：

> 值得注意的是，这个问题的真正原因其实是运行时没有加载配置。

改写后：

> 运行时没有加载配置，因此请求使用了默认值。

## 删除机械反转

改写前：

> 这不是代码问题，而是流程问题。

改写后：

> 发布流程跳过了生成配置文件的步骤；当前代码读取的是上一次发布留下的文件。

## 明确责任人

改写前：

> 该方案已经被确认，并将在后续得到落实。

改写后：

> 项目 owner 在 issue #123 中确认该方案。平台组将在 7 月 30 日前修改部署脚本。

## 删除空泛判断

改写前：

> 这一改进将全方位提升研发效率，意义十分重大。

改写后：

> 修改后，agent 可以直接运行仓库已有的测试入口，不再等待维护者补充命令。
