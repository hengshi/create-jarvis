# Construction evidence

Store task-local, revisitable evidence packets here. Each packet names its source pointer, observed revision/time, redaction boundary and the work card that consumes it.

Do not store credentials, unredacted source dumps or a second copy of durable Company/repo knowledge.
